id: list_scheduled_events
description: "Staff-only scheduled event list"
connection: calendly
method: GET
path: "/api/v2/scheduled_events"
timeout_seconds: 15
pagination:
  style: query
  limit_param: limit
  default_limit: 20
  max_limit: 50
response:
  items: collection
access:
  surfaces:
    - staff
