# Project Tracker (runtime fixture)

Metadata-only Marketplace App used for ZIP install / upgrade genericity tests (M14).

```bash
# Build a ZIP from this directory
../../scripts/build_app_zip.sh project-tracker-runtime
```
