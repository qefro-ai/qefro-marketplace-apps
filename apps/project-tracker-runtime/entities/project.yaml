id: project
name: Project
fields:
  title: { type: string, required: true }
  status: { type: enum, enum_values: [active, on_hold, done], required: true }
  budget: { type: number }
  owner: { type: string }
