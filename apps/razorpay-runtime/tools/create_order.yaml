id: create_order
description: "Staff-only create a Razorpay order"
connection: razorpay
method: POST
path: "/v1/orders"
timeout_seconds: 15
body:
  amount: "{amount}"
  currency: "{currency}"
access:
  surfaces:
    - staff
