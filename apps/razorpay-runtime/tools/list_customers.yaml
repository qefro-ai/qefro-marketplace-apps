id: list_customers
description: "Staff-only customer list on the workspace HTTP connection"
connection: razorpay
method: GET
path: "/v1/customers"
timeout_seconds: 15
pagination:
  style: query
  limit_param: limit
  default_limit: 20
  max_limit: 50
response:
  items: items
access:
  surfaces:
    - staff
