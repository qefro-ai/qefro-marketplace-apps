id: refund
name: Refund
description: "Refund against a payment"
fields:
  - name: amount
    type: integer
  - name: status
    type: enum
    enum_values:
      - pending
      - processed
      - failed
