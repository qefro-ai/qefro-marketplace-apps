id: list_my_customers
description: "List Razorpay customers for the authenticated conversation Person only. Do not pass email, phone, person_id, or customer_id."
connection: razorpay
method: GET
path: "/v1/customers"
timeout_seconds: 15
query:
  email: "{person.email}"
pagination:
  style: query
  limit_param: limit
  default_limit: 20
  max_limit: 50
response:
  items: items
access:
  surfaces:
    - customer
identity:
  require_any:
    - person.email
  collect: email_otp
ownership:
  identity: person.email
  paths:
    - email
  also:
    - identity: person.external_id
      paths:
        - id
        - customer
        - customer.id
        - customer_id
    - identity: person.phone
      paths:
        - phone
        - customer.phone
