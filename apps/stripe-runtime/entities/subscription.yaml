id: subscription
name: Subscription
description: "Recurring subscription"
allocate_code:
  prefix: S-
  start: 1001
fields:
  - name: customer_email
    type: email
  - name: status
    type: enum
    enum_values:
      - incomplete
      - active
      - past_due
      - canceled
      - unpaid
  - name: person_id
    type: person
    ref_entity: person
    description: "Existing Qefro Person identity"
