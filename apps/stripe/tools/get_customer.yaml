id: get_customer
description: "Staff-only customer fetch by Stripe id from a previous list"
connection: stripe
method: GET
path: "/v1/customers/{id}"
timeout_seconds: 15
response:
  item: customer
required_scopes:
  - read_write
access:
  surfaces:
    - staff
